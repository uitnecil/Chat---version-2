import { ConsoleItPipe } from './console-it.pipe';

describe('ConsoleItPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsoleItPipe();
    expect(pipe).toBeTruthy();
  });
});
